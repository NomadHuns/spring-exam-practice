<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="shop.mtcoding.scripttestcoding.model.BoardRepository">
   <select id="findAll" resultType="shop.mtcoding.scripttestcoding.model.Board">
      select * from board_tb
   </select>
   <select id="findById" resultType="shop.mtcoding.scripttestcoding.model.Board">
      select * from board_tb where id = #{id}
   </select>
   <delete id="deleteById">
      delete from board_tb where id = #{id} 
   </delete>
   <update id="updateById">
      update board_tb set title = #{title} where id = #{id}
   </update>
   <insert id="insert">
      insert into board_tb(user_id, title, created_at) values(#{userId}, #{title}, now())
   </insert>
</mapper>